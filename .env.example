# =============================================================================
# India Tax Harvesting Tool - Environment Variables
# =============================================================================
# Copy this file to .env and fill in your actual values:
#   cp .env.example .env
#
# IMPORTANT: Never commit your .env file to version control!
# =============================================================================

# -----------------------------------------------------------------------------
# SUPABASE CONFIGURATION
# -----------------------------------------------------------------------------
# Supabase provides the backend database and authentication for this app.
#
# To get these values:
# 1. Create a free account at https://supabase.com
# 2. Create a new project (or use an existing one)
# 3. Go to Project Settings > API
# 4. Copy the "Project URL" and "anon public" key
#
# Note: The anon key is safe to expose in client-side code as Row Level Security
# (RLS) policies protect your data.
# -----------------------------------------------------------------------------

# Your Supabase project URL (e.g., https://abcdefghijklmnop.supabase.co)
PUBLIC_SUPABASE_URL=your_project_url

# Your Supabase anonymous/public API key (starts with 'eyJ...')
# This is the "anon" key, NOT the "service_role" key
PUBLIC_SUPABASE_ANON_KEY=your_anon_key

# -----------------------------------------------------------------------------
# KITE CONNECT CONFIGURATION (Optional)
# -----------------------------------------------------------------------------
# Kite Connect API is used for executing trades via Kite Publisher.
# This app uses "basket mode" which only requires the API key - no secret needed.
#
# To get your API key:
# 1. Go to https://developers.kite.trade/
# 2. Sign in with your Zerodha credentials
# 3. Create a new app (or use existing)
# 4. Copy the "API Key" from your app details
#
# Note: You DON'T need:
# - API Secret (not required for Publisher)
# - Access Token (not required for Publisher)
# - Any paid subscription (Publisher is free)
#
# If not provided, the app will still work but you'll need to manually
# place orders on Kite instead of using the one-click basket feature.
# -----------------------------------------------------------------------------

# Your Kite Connect API key (alphanumeric string)
PUBLIC_KITE_API_KEY=your_kite_api_key
